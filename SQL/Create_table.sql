CREATE TABLE sales_data (
    order_date DATE,
    segment VARCHAR(50),
    country VARCHAR(100),
    market VARCHAR(50),
    order_id VARCHAR(30),
    category VARCHAR(50),
    product_name TEXT,
    price NUMERIC(10,3),
    quantity INTEGER
);